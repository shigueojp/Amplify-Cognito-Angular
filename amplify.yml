version: 0.1
frontend:
  phases:
    # IMPORTANT - Please verify your build commands
    build:
      commands:
        ["npm install", "sh ./headless_init_env.sh", "npm run-script build"]
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: dist/POC/
    files:
      - "**/*"
  cache:
    paths: []
